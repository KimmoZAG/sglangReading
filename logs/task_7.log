全部自检通过。文档已完成。

**完成确认**
- 目标文件已完全重写，无 `TODO: 待子任务填充（占位）` 行（grep 计数 0）。
- 字数 16918 字符（≥2600）。
- 真实证据锚点 **19 处**（≥8），全部经 `Read` 实测行号确认（含 `base_runner.py:448-L459`、`671-L681` 等亦已直接 Read 校验）。
- 含 1 个 Mermaid 流程图（`_forward_raw` 的 graph replay / eager / piecewise 分支）。
- 结构覆盖 What / Why / How / 边界与坑 四节。
- OPEN 问题已写入 `docs/appendix/_openq_model-runner.md`（正文对应处用 `> **[OPEN]**` 标注）。

**核心内容要点**
- `ModelRunner` 职责：`forward` → `_forward_raw` 按 `forward_mode` 与 shape 在 decode CUDA Graph replay / prefill piecewise graph / eager 三条路径分发（锚 `model_runner.py:1654`）。
- `ForwardBatch.init_new` 的 prefill/decode 差异：extend 专属字段在 decode/idle 置空、位置计算路径不同（锚 `forward_batch_info.py:738`、`:767`、`:905`）。
- CUDA Graph 捕获时机：启动期、首请求前，`capture_cuda_graphs` 严格按 Eager→prefill→decode 顺序，共享静态缓冲（锚 `cuda_graph_setup.py:89`）。
- replay 条件：`DecodeCudaGraphRunner.can_run_graph` 的 shape/DP/encoder/ngram 闸门（锚 `decode_cuda_graph_runner.py:583`）。
- shape 约束应对：padding + piecewise/breakable 分段图 + `tc_piecewise`（torch.compile）后端。
- torch.compile 取舍：CPU 解码与 prefill `tc_piecewise` 主用，decode 默认 CUDA Graph；并标注了一处未完全串联的 OPEN 疑问。

需要我继续补充其他 deep-dive 文档，或针对某条路径（如 piecewise 图的具体分段录制）再深挖吗？
