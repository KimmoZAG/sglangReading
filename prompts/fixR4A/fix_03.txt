你是 SGLang 文档修复工程师。只改【一个】文档文件里的源码锚点/深度/mermaid 错误，不改动其它内容，不编造。

【唯一事实来源 SSOT】/home/kimmo/develop/sglang（commit e1c4db9621f7c4203ee9becd5d5456d4e6bf54f7）。所有修正必须先用 grep/Read 在 SSOT 实测确认。

【目标文件（绝对路径）】/home/kimmo/develop/sglangReading/docs/appendix/glossary.md

【本文件被评阅出的问题清单】

### 问题 1（SEVERITY=medium TYPE=anchor_fake）
- DETAIL: L47 把 `python/sglang/srt/managers/scheduler.py:81` 用作 "Disaggregated Prefill（PD 分离）" 的证据锚点。实测 :81 是 `from sglang.srt.disaggregation.prefill import (...)` 的**import 行**，并非 PD 分离入口；真实入口 `init_disaggregation` 在 :1278，bootstrap 队列构造在 :1392（该 :1392 同条已正确引用）。
- SUGGESTED_FIX: 将 :81 改为 `scheduler.py:1278`（或 :1392），保留 :1392。

### 问题 2（SEVERITY=low TYPE=anchor_drift）
- DETAIL: L32 `radix_cache.py:307-L308` 标 (page_size)。实测 :307 = `self.token_to_kv_pool_allocator = ...`；:308 = `self.page_size = ...`。区间起点并非 page_size。
- SUGGESTED_FIX: 单引 `:308`，或区间标注为 `token_to_kv_pool_allocator / page_size`。

### 问题 3（SEVERITY=low TYPE=anchor_drift）
- DETAIL: L49 `radix_cache.py:306-L307` 命名为 `req_to_token_pool / token_to_kv_pool`。实测 :306 = `self.req_to_token_pool`、:307 = `self.token_to_kv_pool_allocator`（真实符号带 `_allocator` 后缀）。
- SUGGESTED_FIX: 文档命名改为 `token_to_kv_pool_allocator` 以与 SSOT 一致。

### 问题 4（SEVERITY=low TYPE=anchor_drift）
- DETAIL: L124 `parallel_state.py:2286` 标 `initialize_model_parallel`。实测 `def initialize_model_parallel(` 在 **:2285**，:2286 为首个参数行。
- SUGGESTED_FIX: `parallel_state.py:2285`。

### 问题 5（SEVERITY=low TYPE=anchor_drift）
- DETAIL: L38、L205 把 `parallel_state.py:2289` 当作 `attn_dp_size`。实测 :2289 = 函数参数 `attention_data_parallel_size: int = 1`；局部 `attn_dp_size = attention_data_parallel_size` 定义在 **:2451**。
- SUGGESTED_FIX: :2289 标注为参数 `attention_data_parallel_size`；`attn_dp_size` 引用改标 :2451。

### 问题 6（SEVERITY=low TYPE=anchor_drift）
- DETAIL: L200 写 `initialize_model_parallel` 断言 `world_size == tp_size * pp_size`。实测 `parallel_state.py:2360` 代码为 `if world_size != tensor_model_parallel_size * pipeline_model_parallel_size:`（即断言**不等则报错**），方向与文档描述相反（行号正确，语义被倒置）。
- SUGGESTED_FIX: 改写为 "assert `world_size == tp_size * pp_size`，否则 raise RuntimeError"。


【修复步骤（严格执行）】
1) 用 Read 工具读完整篇 /home/kimmo/develop/sglangReading/docs/appendix/glossary.md。
2) 对清单里每一条：
   - 用 `grep -n "<符号>" /home/kimmo/develop/sglang/python/sglang/srt/<对应文件>` 找到该符号的真实定义行（若 SUGGESTED_FIX 已给正确锚点，仍要用 grep 复核行号是否真实）。
   - 在文档里定位那条错误的锚点字符串（从 DETAIL 引用的错误锚点原文），用 Edit 工具替换为【已 grep 复核】的正确锚点或修订文本。
   - 规则：
     * 路径错误（如 engine.py 应为 scheduler.py）：改正路径与行号。
     * 区间颠倒（如 986-917）：改为升序 917-986。
     * 符号名不匹配（如 wait_for_ready 实为 _wait_for_scheduler_ready）：改正符号名，行号保留建议值并复核。
     * 缺前缀的裸文件名（如 `server_args.py:L218`）：补成完整相对 SSOT 路径 `python/sglang/srt/server_args.py:L218`（若该模块确实在 srt 下；用 ls/grep 确认存在）。
     * 占位/非精确锚点（如 `Lxxx+`）：用 grep 算出真实行号后替换。
     * 深度问题（shallow）：在对应段落补充真实代码路径、行号锚点与具体权衡，删除无信息量空话；必须基于 SSOT 实测，不得编造。
     * mermaid 虚构类名（mermaid_fake）：把图中不存在的类/函数名改为 SSOT 中真实存在的名称（先 grep `class <名>`/`def <名>` 确认）。
3) 严禁新增/删除非必要内容；保持 mermaid 代码块完整、不引入新 TODO。
4) 不运行 mkdocs build（本机沙箱会误报）；只保证事实准确。

【验收（完成后）】
- 报告你改了哪些地方（旧→新）到文件：/home/kimmo/develop/sglangReading/docs/../reports/fixR4_report_appendix_glossary.md.md
- 再次 grep 复核：每条修正后的锚点行号处确为文中引用的符号。
