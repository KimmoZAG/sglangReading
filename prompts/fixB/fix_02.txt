你是 SGLang 文档修复工程师。只改【一个】文档文件里的源码锚点错误，不改动其它内容，不编造。

【唯一事实来源 SSOT】/home/kimmo/develop/sglang（commit e1c4db9621f7c4203ee9becd5d5456d4e6bf54f7）。所有修正必须先用 grep/Read 在 SSOT 实测确认。

【目标文件（绝对路径）】/home/kimmo/develop/sglangReading/docs/deep-dive/observability.md

【本文件被评阅出的问题清单】

### 问题 1（SEVERITY=low TYPE=anchor_drift）
- DETAIL: "输出 .trace.json.gz 的 Chrome trace（profiler_manager.py:220-265）" —— `.trace.json.gz` 字符串实际在 L197 与 L338；220-265 是 `activities`（MEM/RPD/CUDA_PROFILER）处理段，区间略偏。
- SUGGESTED_FIX: trace 输出锚点改为 `python/sglang/srt/managers/scheduler_components/profiler_manager.py:197` 与 `:338`；MEM/RPD/CUDA_PROFILER 扩展单独标 `:252-L264`。

### 问题 2（SEVERITY=low TYPE=shallow）
- DETAIL: "## Benchmark 工具链" 段落（L118-130）以目录罗列 `serving.py`/`offline_throughput.py`/`bench_adaptive_speculative.py` 等文件名与一句话功能为主，缺乏与其它章节同深度的代码路径/调用链论证，相对信息量偏低（但每条均附文件锚点，未达"凑字数"程度）。
- SUGGESTED_FIX: 可补 1~2 个代表性 benchmark 的"参数如何流入 ServerArgs / 如何读取 /metrics"的关键调用链，或明确标注为"索引性附录"以免与深度章节混同。

---

## TOP5（按 SEVERITY 排序，最该优先修）

1. **[medium] lora-multimodal.md — `get_new_expanded_mm_items` 路径错**（anchor_fake）：读者会去错误的 `base_processor.py` 找定义，应改指 `managers/mm_utils.py:1090`。
2. **[medium] lora-multimodal.md — `get_mm_items_offset` 漂移 384 行**（anchor_drift）：定义实际在 L1297，非 L1681。
3. **[medium] sampling.md — `merge_batch` 区间起点漂移 26 行 + `__len__` 符号/行双错位**（anchor_drift）：改 L388 与 L236。
4. **[medium] disaggregation.md — `match_prefix_for_req` 符号名笔误**（anchor_drift）：真实函数 `_match_prefix_and_lock`（L561）。
5. **[low] observability.md — profiler trace.json.gz 锚点区间略偏 + Benchmark 段落偏浅**（anchor_drift / shallow）：trace 锚点改 L197/L338，Benchmark 段可补调用链或降级为索引附录。


【修复步骤（严格执行）】
1) 用 Read 工具读完整篇 /home/kimmo/develop/sglangReading/docs/deep-dive/observability.md。
2) 对清单里每一条：
   - 用 `grep -n "<符号>" /home/kimmo/develop/sglang/python/sglang/srt/<对应文件>` 找到该符号的真实定义行（若 SUGGESTED_FIX 已给正确锚点，仍要用 grep 复核行号是否真实）。
   - 在文档里定位那条错误的锚点字符串（从 DETAIL 引用的错误锚点原文），用 Edit 工具替换为【已 grep 复核】的正确锚点。
   - 规则：
     * 路径错误（如 engine.py 应为 scheduler.py）：改正路径与行号。
     * 区间颠倒（如 986-917）：改为升序 917-986。
     * 符号名不匹配（如 wait_for_ready 实为 _wait_for_scheduler_ready、match_prefix_for_req 实为 _match_prefix_and_lock）：改正符号名，行号保留建议值并复核。
     * 缺前缀的裸文件名（如 `server_args.py:L218`）：补成完整相对 SSOT 路径 `python/sglang/srt/server_args.py:L218`（若该模块确实在 srt 下；用 ls/grep 确认存在）。
     * 占位/非精确锚点（如 `Lxxx+`）：用 grep 算出真实行号后替换。
3) 严禁新增/删除非锚点内容；保持 mermaid 代码块完整、不引入新 TODO。
4) 不运行 mkdocs build（本机沙箱会误报）；只保证锚点真实。

【验收（完成后）】
- 报告你改了哪些锚点（旧→新）到文件：/home/kimmo/develop/sglangReading/docs/../reports/fix_report_deep-dive_observability.md.md
- 再次 grep 复核：每条修正后的锚点行号处确为文中引用的符号。
