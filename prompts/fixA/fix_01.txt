你是 SGLang 文档修复工程师。只改【一个】文档文件里的源码锚点错误，不改动其它内容，不编造。

【唯一事实来源 SSOT】/home/kimmo/develop/sglang（commit e1c4db9621f7c4203ee9becd5d5456d4e6bf54f7）。所有修正必须先用 grep/Read 在 SSOT 实测确认。

【目标文件（绝对路径）】/home/kimmo/develop/sglangReading/docs/architecture/overview.md

【本文件被评阅出的问题清单】

### 问题 1（SEVERITY=high TYPE=anchor_fake）
- DETAIL: `python/sglang/srt/entrypoints/engine.py:2003` 引用的 `self.recv_from_tokenizer = rust_server` 在 engine.py 中不存在：engine.py 实际仅 1846 行（:2003 越界），且 `grep -nE "rust_server|recv_from_tokenizer" engine.py` 全文无命中。该锚点既越界又符号缺失。
- SUGGESTED_FIX: 该符号真实位于 `python/sglang/srt/managers/scheduler.py:2003`（`self.recv_from_tokenizer = rust_server` 确在此处）。请将锚点改为 `python/sglang/srt/managers/scheduler.py:2003`。

### 问题 2（SEVERITY=medium TYPE=anchor_drift）
- DETAIL: `python/sglang/srt/entrypoints/engine.py:1762-1791` 标注为 `wait_for_ready`，但 engine.py 中 `wait_for_ready` 方法定义于第 939 行；:1762-1791 实为模块级函数 `_wait_for_scheduler_ready`（其内含 `poll(timeout=5.0)` 阻塞等待）。符号名与行号不匹配，读者按 `wait_for_ready` 在 1762 找不到该方法。
- SUGGESTED_FIX: 若指 `wait_for_ready` 则改 `engine.py:939`；若指阻塞等待逻辑则改 `engine.py:1762`（并更正符号名为 `_wait_for_scheduler_ready`）。

### 问题 3（SEVERITY=low TYPE=anchor_drift）
- DETAIL: `python/sglang/srt/managers/scheduler.py:986-917` 区间起止颠倒（986 > 917），属无效区间。该处意图指向 `init_model_worker`（定义于 986）与 `self.tp_worker = TpModelWorker(...)`（位于 917）。
- SUGGESTED_FIX: 改为 `python/sglang/srt/managers/scheduler.py:917-986`（或仅 `986`）。


【修复步骤（严格执行）】
1) 用 Read 工具读完整篇 /home/kimmo/develop/sglangReading/docs/architecture/overview.md。
2) 对清单里每一条：
   - 用 `grep -n "<符号>" /home/kimmo/develop/sglang/python/sglang/srt/<对应文件>` 找到该符号的真实定义行（若 SUGGESTED_FIX 已给正确锚点，仍要用 grep 复核行号是否真实）。
   - 在文档里定位那条错误的锚点字符串（从 DETAIL 引用的错误锚点原文），用 Edit 工具替换为【已 grep 复核】的正确锚点。
   - 规则：
     * 路径错误（如 engine.py 应为 scheduler.py）：改正路径与行号。
     * 区间颠倒（如 986-917）：改为升序 917-986。
     * 符号名不匹配（如 wait_for_ready 实为 _wait_for_scheduler_ready、match_prefix_for_req 实为 _match_prefix_and_lock）：改正符号名，行号保留建议值并复核。
     * 缺前缀的裸文件名（如 `server_args.py:L218`）：补成完整相对 SSOT 路径 `python/sglang/srt/server_args.py:L218`（若该模块确实在 srt 下；用 ls/grep 确认存在）。
     * 占位/非精确锚点（如 `Lxxx+`）：用 grep 算出真实行号后替换。
3) 严禁新增/删除非锚点内容；保持 mermaid 代码块完整、不引入新 TODO。
4) 不运行 mkdocs build（本机沙箱会误报）；只保证锚点真实。

【验收（完成后）】
- 报告你改了哪些锚点（旧→新）到文件：/home/kimmo/develop/sglangReading/docs/../reports/fix_report_architecture_overview.md.md
- 再次 grep 复核：每条修正后的锚点行号处确为文中引用的符号。
